<h1 class="new">New Book</h1>
<%= form_for(@book) do |f| %>
	<h4 class="new-title">Title</h4>
	<%= f.text_field :title %>
	<h4 class="new-body">Body</h4>
	<%= f.text_area :body %>

	<p class="new-create"><%= f.submit 'create book' %></p>

<% end %>